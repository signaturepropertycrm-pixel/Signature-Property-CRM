rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to read/write to the avatars folder
    match /avatars/{agencyId}/{userId} {
      allow read, write: if request.auth != null;
    }
  }
}
